{
  "name": "eclair_paikka",
  "version": "1.0.0",
  "scripts": {
    "build": "postcss src/assets/css/style.css -o dist/style.css --verbose && cpx 'src/**/*' dist/ --verbose"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.20",
    "cpx": "^1.5.0",
    "tailwindcss": "^3.4.9",
    "postcss": "^8.4.41",
    "postcss-cli": "^11.0.0"
  }
}
